import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:ita-office-locations/index', 'ItaOfficeLocationsIndexController', {
  // Specify the other units that are required for this test.
  needs: ['controller:ita-office-locations']
});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
