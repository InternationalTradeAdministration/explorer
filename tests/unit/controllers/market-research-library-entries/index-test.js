import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:market-research-library-entries/index', 'MarketResearchLibraryEntriesIndexController', {
  // Specify the other units that are required for this test.
  needs: ['controller:market-research-library-entries']
});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
