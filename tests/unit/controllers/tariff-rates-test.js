import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:tariff-rates', 'TariffRatesController', {});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
